# Public browser configuration only. Never put JWTs, passwords, provider keys,
# or other secrets in VITE_* values because Vite embeds them in the bundle.
VITE_MEDIA_VAULT_API_URL=https://api.example.com
